FIT4LIFE V12 RELEASE CHECKS — UPDATED 2026-08-17

PASSED LOCALLY
- JavaScript syntax: every .js file passed Node syntax validation.
- JSON: manifest.webmanifest and vercel.json parsed successfully.
- HTML: 601 unique element IDs; no duplicate IDs.
- Assets: all 21 local HTML references resolve to included files.
- Load order: role governance loads before application initialization.
- Redirect behavior: future generated *.vercel.app hosts redirect to the production
  root; production and localhost do not redirect; query strings and URL hashes remain.
- Update delivery: app assets are version-tagged, the app shell revalidates, service
  worker updates bypass cache, and JavaScript/CSS use network-first service-worker
  handling with offline fallback.
- V5 invariants: owner-only client preview, owner approval decisions, requester
  self-approval guard, V5 database script, removed Business navigation, and preserved
  trainer/client set-count behavior are present.
- V9 calendar saves: follow-up, reschedule, cancel, and normal calendar creation
  passed live click-through tests and persisted after reload.
- V9 connected saves: profile edit, owner request, coaching note, trainer reply, and
  Settings sender identity passed live click-through and reload tests.
- 226 application functions referenced by inline click handlers resolved; the only
  unmatched tokens were browser/JavaScript built-ins used inside valid handlers.
- V10 rendered the permanent blue F4L sign over the black rock wall without blocking
  page controls.
- The earlier V10 preset browser run activated each matching root theme and selected
  state. V12 intentionally narrows the published set to holidays only.
- The V10 browser run reported zero runtime console errors.
- V9 remains preserved separately; V10 is a new versioned folder.
- V11 visually confirmed separate F, 4, and L spacing with no overlap.
- The V11 sign renders with a dark metallic face and separate blue neon edge.
- The role-selection title and explanation remain readable inside their translucent
  dark panel even where the F4L sign crosses behind the panel.
- Trainer-side controls stayed clickable over the layered sign.
- The final V11 visual run reported zero runtime console errors.
- V12 JavaScript syntax checks passed for every application, API, cloud-sync, and
  service-worker file after the immersive-theme integration.
- V12 JSON parsing passed for manifest.webmanifest and vercel.json.
- V12 still contains 601 unique HTML IDs with no duplicate IDs.
- All six final holiday-scene JPEGs resolve locally; together they add about 1.3 MB.
- The V12 service-worker shell contains every theme scene and uses its own cache name.
- The app shell and all JavaScript/CSS references use the V12 cache-busting tag.
- Theme artwork is click-through, removed from printed pages, and its particle layer
  is disabled when the operating system requests reduced motion.
- The publisher requires an authenticated owner and a connected organization before
  it calls the owner-only organization-settings RPC.
- Trainer accounts receive only the published-theme summary and owner-request action;
  they cannot preview or publish theme alternatives.
- Organization settings refresh through Supabase Realtime and also on app open,
  foreground return, reconnection, and a quiet 60-second fallback check.
- Owner membership now exposes its organization ID before the secondary settings
  read, and the theme publisher retries the membership connection once if necessary.
- Sign-out clears the exported and internal organization context, preventing a later
  account from inheriting the previous owner's publishing target.
- The August 17 r3 cache tag is consistent across the HTML shell and service worker.

REQUIRED BEFORE LIVE USE
1. Upload this folder's contents to the same repository and Vercel project.
2. Deploy/promote the intended commit to Production.
3. Run supabase-v5-role-boundaries.sql in the connected Supabase SQL Editor.
4. Enable Vercel Authentication + Standard Protection in Project Settings >
   Deployment Protection.
5. Test the production URL in a private browser window as an owner, trainer, and
   client account.
6. Test one future generated deployment URL and one historical generated URL.

LIVE V12 THEME CHECKS
- [ ] Confirm the metallic-black F4L sign is visible in the center of the rock wall.
- [ ] Sign in as an owner and open Settings > Themes, appearance & gym setup.
- [ ] Confirm the account indicator shows Owner and no “shared gym connection”
      warning appears when selecting the first theme after sign-in.
- [ ] Click each preset and confirm the sign outline, top-bar badge, scene thumbnail,
      bottom 3D scene, and matching ambient particles all change together.
- [ ] Confirm New Year, Halloween, Thanksgiving, Christmas, Valentine's, and
      Independence Day each display their matching scene and ambient treatment.
- [ ] Refresh the page and confirm the selected preset remains active.
- [ ] Keep a trainer and client device open, publish from an owner device, and confirm
      both other devices change without signing out or reloading.
- [ ] Sign in as a trainer and confirm the picker is absent and change is request-only.
- [ ] Return to Neon blue when no holiday theme should be active.

NOT PERFORMED BY THE LOCAL CHECKS
- No live Vercel deployment was created or promoted.
- No SQL was executed against the production Supabase database.
- No production owner, trainer, or client account was modified.
- No post-integration browser screenshot run was performed for V12; complete the live
  V12 theme checklist after uploading the release to the Production project.
- Historical immutable Vercel deployment pages were not deleted. Standard Protection
  is the Vercel-side control that prevents those old snapshots from remaining public.
